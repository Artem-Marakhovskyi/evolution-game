import '../../../evolution_heart.dart';
import '../cards_stack.dart';

class PhaseYoungGeneration {
  final List<Player> _players;
  final CardsStack _cardsStack;
  PhaseYoungGeneration(this._players, this._cardsStack);

  void pushCards() {
    // if (_cardsStack.canTake(count))
    // _players.forEach((element) {
    //   _cardsStack
    // });
  }
}
